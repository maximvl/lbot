language: lisp
sudo: required

env:
  matrix:
    - LISP=sbcl
    # - LISP=ccl
    # - LISP=clisp
    # - LISP=ecl

install:
  - curl -L https://github.com/luismbo/cl-travis/raw/master/install.sh | sh

script:
  - cl -e '(push \#p"." ql:*local-project-directories*)
           (ql:quickload :lbot-test)
           (unless (prove:run :lbot-test)
              (uiop:quit 1))'
